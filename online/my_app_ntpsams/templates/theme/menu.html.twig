<style>
    .cart-badge {
        /* Positionnement absolu par rapport au lien <a> parent */
        position: absolute;
    }


</style>
<style>
    /* ------------------------------------------------------------- */
    /* STYLES DESKTOP (Par Défaut : Affiche le TEXTE) */
    /* ------------------------------------------------------------- */

    /* Par défaut, cacher l'icône sur les grands écrans */
    .custom-responsive-btn .icon-mobile-only {
        display: none;
    }

    /* Par défaut, afficher le texte sur les grands écrans */
    .custom-responsive-btn .text-desktop-only {
        display: inline;
    }


    /* ------------------------------------------------------------- */
    /* STYLES MOBILE (< 768px : Affiche l'ICÔNE seule) */
    /* ------------------------------------------------------------- */

    @media (max-width: 767.98px) {

        /* Sur mobile, masquer le texte */
        .custom-responsive-btn .text-desktop-only {
            display: none;
        }

        /* Sur mobile, afficher l'icône */
        .custom-responsive-btn .icon-mobile-only {
            display: inline-block;

            /* Ajustements de style pour l'icône seule */
            line-height: 1;
            margin-left: -10px;
            font-size: 1.5em; /* Rendre l'icône plus visible */
        }

        /* Optionnel : centrer et adapter la taille du bouton A lui-même sur mobile */
        .custom-responsive-btn {
            /* Adapter le padding pour qu'il soit plus petit et carré autour de l'icône */
            padding: 5px;
            width: 40px; /* Taille fixe pour l'icône seule (ajuster si nécessaire) */
            height: 40px;
            line-height: 20px; /* Centrer verticalement l'icône */
            text-align: center;
            border-radius: 5px; /* S'assurer qu'il a une forme propre */
        }
    }
</style>
{% set cart_data = app.session.get('cart', []) %}
{# 🥇 Compte le nombre d'éléments de niveau supérieur dans le tableau. #}
{% set total_items = cart_data|length %}

<style>
    .sitename {
        font-size: 100% !important;
    }
</style>



<header id="header" class="header d-flex align-items-center fixed-top">
    <div class="container position-relative d-flex align-items-center justify-content-between">

        <a href="{{ url('app_home_ln') }}" class="logo d-flex align-items-center me-auto me-xl-0" title=" {{ func_pageinfo()['titlesite'] }}">
            <img src="{{asset('assets/img/pageinfo/'~func_pageinfo()['logo'])}}" alt="image {{ func_pageinfo()['titlesite'] }}" class="img-fluid" width="100%">
        </a>

        <div class="mobile-utils d-flex righ gap-3 me-3 d-xl-none">
            <a href="{{ url('app_shop_card_liste') }}" class="text-light p-0" title="Panier">
                {% if total_items > 0 %}
                    <span class="cart-badge badge text-bg-danger rounded-pill">
                    {{ total_items }}
                </span>
                {% endif %}
                <i class="bi bi-bag fs-5"></i>
            </a>


            <a href="{{ url('app_login') }}" class="text-light p-0"  title="{% trans %}login_nav{% endtrans %}"
            ><i class="bi bi-person-circle fs-5"></i></a>

            {#            <a href="{{ url('app_login') }}" class="text-success-light p-0"  title="{% trans %}login_nav{% endtrans %}"#}
            {#            ><i class="bi bi-person-circle fs-5"></i></a>#}


            {% include 'theme/language.html.twig' %}
        </div>

        <nav id="navmenu" class="navmenu">



            <ul>
                <li>
                    <a href="{{ url('app_home_ln') }}"
                       class="{% if app.request.schemeAndHttpHost ~ app.request.requestUri == url('app_home_ln') %}active{% endif %}"
                    >
                        {% trans %}Home{% endtrans %}
                    </a>

                </li>



                <li class="dropdown"><a href="#"><span>About</span> <i class="bi bi-chevron-down toggle-dropdown"></i></a>
                    <ul>
                        <li><a href="{{ url('app_about_home',{'id':'1'}) }}">Who we are?</a></li>
                        <li><a href="{{ url('app_about_home',{'id':'2'}) }}">Why choose us?</a></li>
                    </ul>
                </li>

                <li><a href="{{ url('app_service_liste') }}">Services</a></li>


                <li><a href="{{url('app_shop_liste')}}">shop</a></li>


                <li><a href="{{url('app_contact_form')}}">Contact</a></li>
            </ul>
            <i class="mobile-nav-toggle d-xl-none bi bi-list"></i>
        </nav>

        <div class="header-util d-none d-xl-flex align-items-center gap-4 ms-3">
            <a href="{{ url('app_shop_card_liste') }}" class="text-light p-0" title="Panier">
                {% if total_items > 0 %}
                    <span class="cart-badge badge text-bg-danger rounded-pill">
                    {{ total_items }}
                </span>
                {% endif %}
                <i class="bi bi-bag fs-5"></i>
            </a>



            <a href="{{ url('app_login') }}" class="text-light p-0"  title="{% trans %}login_nav{% endtrans %}"><i class="bi bi-person-circle fs-5"></i></a>
            {#            <a href="{{ url('app_admin_admin') }}" class="text-light p-0"  ><i class="bi bi-person-circle fs-5"></i></a>#}

            {% include 'theme/language.html.twig' %}
        </div>

        <a class="cta-btn custom-responsive-btn"
           target="_blank"
           href="https://app.squareup.com/appointments/book/7jwdcckk8siagh/LGJ2PB5YW3M0K/start"
           rel="nofollow"
        >
            {# L'icône que nous voulons afficher sur téléphone #}
            <i class="bi bi-calendar-check icon-mobile-only"></i>

            {# Le texte que nous voulons afficher sur PC #}
            <span class="text-desktop-only">
                Book A Consult
            </span>
        </a>
        <!-- Start Square Appointments Embed code -->

    </div>
</header>

